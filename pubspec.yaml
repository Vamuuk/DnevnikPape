name: dnevnik
description: Дневник Берика
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
    
  # Основные зависимости (кроссплатформенные)
  shared_preferences: ^2.2.2
  table_calendar: ^3.0.9
  
  # Голосовые заметки (совместимые с iOS и Android)
  flutter_sound: ^9.2.13
  permission_handler: ^11.0.1
  path_provider: ^2.1.1
  
  # Локализация
  flutter_localizations:
    sdk: flutter
  intl: ^0.19.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true